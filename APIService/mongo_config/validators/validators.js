const blocksValidator = JSON.parse(cat(pwd() + "/validators/blocks_validator.json"));
const bookingsValidator = JSON.parse(cat(pwd() + "/validators/bookings_validator.json"));
const coursesValidator = JSON.parse(cat(pwd() + "/validators/courses_validator.json"));
const identityValidator = JSON.parse(cat(pwd() + "/validators/identity_validator.json"));
const tokensValidator = JSON.parse(cat(pwd() + "/validators/tokens_validator.json"));
